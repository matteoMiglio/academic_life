<h1><%= @course.name %></h1>
<%= render "message_boards/menu", :type => "posts" %>
<%= render "new" %>

<ul class="posts">
  <%= render @posts %>
</ul>

<%= will_paginate @posts, renderer: WillPaginate::ActionView::Bootstrap4LinkRenderer %>
